const producto = {
    nombre: "Monitor de 34 pulgadas",
    precio: 300,
    desponible: true
}

console.log(producto);

console.log(producto.nombre);
console.log(producto.precio);
console.log(producto.desponible);

console.log(producto['nombre']);

